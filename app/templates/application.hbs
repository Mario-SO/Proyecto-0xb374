<div class="container">
    <div class="menu">
        {{#link-to "index"}}
        <h1>
            <em>PROYECTO 0xb374</em>
        </h1>
        {{/link-to}}
        <div class="links">
            {{#link-to "about" class="menu-about"}}
            About
            {{/link-to}}
            {{#link-to "contact" class="menu-contact"}}
            Contact
            {{/link-to}}
        </div>
    </div>
    <div class="body">
        <!-- {{outlet}} es donde se va a poner el contenido que carguemos -->
        {{outlet}}
    </div>
</div>